---
/**
 * CTA 섹션 (견적 요청)
 * @TASK P2-S1-T10
 * @SPEC docs/planning/06-screens.md#1.11-견적-요청-섹션
 *
 * 기능:
 * - 섹션 헤더: "지금 바로 시작하세요"
 * - 서브 카피: 24시간 내 맞춤 견적 안내
 * - CTA 버튼: [무료 견적 받기], [카카오톡 상담하기]
 */

import { COMPANY_EMAIL, KAKAO_CHANNEL_URL } from '../../lib/constants';
---

<section id="cta" class="py-20 md:py-24 hero-gradient relative overflow-hidden">
  <!-- Background Pattern -->
  <div class="absolute inset-0 tech-pattern opacity-20"></div>

  <!-- Blur Effects -->
  <div class="absolute top-0 right-0 w-64 h-64 bg-blue-500/20 rounded-full blur-[100px]"></div>
  <div class="absolute bottom-0 left-0 w-48 h-48 bg-indigo-500/20 rounded-full blur-[80px]"></div>

  <div class="relative z-10 max-w-4xl mx-auto px-6 text-center">
    <!-- Badge -->
    <div class="inline-flex items-center gap-2 px-4 py-2 bg-white/10 rounded-full mb-8 backdrop-blur-sm">
      <span class="material-symbols-outlined text-green-400 text-sm">schedule</span>
      <span class="text-sm font-medium text-white/90">24시간 내 답변 보장</span>
    </div>

    <!-- Title -->
    <h2 class="text-3xl md:text-4xl lg:text-5xl font-bold text-white mb-4 leading-tight">
      지금 바로 시작하세요
    </h2>

    <!-- Subtitle -->
    <p class="text-lg md:text-xl text-white/80 mb-4 leading-relaxed">
      24시간 내 맞춤 견적을 보내드립니다
    </p>
    <p class="text-base text-white/60 mb-10">
      상담은 언제나 무료입니다
    </p>

    <!-- CTA Buttons -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center mb-12">
      <a
        href={`mailto:${COMPANY_EMAIL}?subject=[견적문의] 새봄컨설팅&body=안녕하세요, 견적 문의드립니다.%0A%0A- 회사명:%0A- 제품 종류:%0A- 수출 대상국:%0A- 문의 내용:`}
        class="inline-flex items-center justify-center gap-2 px-8 sm:px-10 py-4 sm:py-5 bg-white text-navy rounded-xl font-bold text-base sm:text-lg hover:shadow-2xl hover:scale-105 transition-all"
      >
        <span class="material-symbols-outlined text-xl">mail</span>
        무료 견적 받기
      </a>
      <a
        href={KAKAO_CHANNEL_URL}
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center justify-center gap-2 px-8 sm:px-10 py-4 sm:py-5 bg-[#FEE500] text-[#3C1E1E] rounded-xl font-bold text-base sm:text-lg hover:shadow-2xl hover:scale-105 transition-all"
      >
        <span class="material-symbols-outlined text-xl">chat</span>
        카카오톡 상담하기
      </a>
    </div>

    <!-- Contact Info -->
    <div class="flex flex-col sm:flex-row items-center justify-center gap-6 text-white/70">
      <a
        href={`mailto:${COMPANY_EMAIL}`}
        class="flex items-center gap-2 hover:text-white transition-colors"
      >
        <span class="material-symbols-outlined text-lg">mail</span>
        <span class="text-sm">{COMPANY_EMAIL}</span>
      </a>
      <span class="hidden sm:inline text-white/30">|</span>
      <div class="flex items-center gap-2">
        <span class="material-symbols-outlined text-lg">schedule</span>
        <span class="text-sm">평일 09:00 - 18:00</span>
      </div>
    </div>
  </div>
</section>
